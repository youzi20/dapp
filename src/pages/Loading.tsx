import { EmptyLoading } from '../components/Empty';

import { Wrapper } from '../styled';

const Loading = () => {
    return <Wrapper>
        <EmptyLoading />
    </Wrapper>
}

export default Loading;
